services:
  web:
    image: nginx:alpine    # 使用轻量级的 Nginx Web 服务器
    ports:
      - "8080:80"          # 将电脑的 8080 端口映射到容器的 80 端口
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html # 【核心】挂载文件